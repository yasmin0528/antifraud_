batch_size: 256
hid_dim: 256
lr: 0.001
wd: 0.0001
n_layers: 2
dropout: [0.2, 0.1]
device: "cuda:0"
early_stopping: 5
seed: 2023
max_epochs: 10
gated: true

dataset: amlsim
amlsim_variant: HI-Small
data_path: data/AMLSIM
split_mode: temporal
amlsim_train_days: 14
amlsim_val_days: 2

results_dir: results
nei_att_heads:
  amlsim: 1
